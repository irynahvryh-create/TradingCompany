<Window x:Class="TradingCompany.WPF.Windows.ProductLogDetails"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Деталі логу" Height="350" Width="400">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Content="Product ID:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding ProductID, UpdateSourceTrigger=PropertyChanged}"/>

        <Label Content="Old Price:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding OldPrice, UpdateSourceTrigger=PropertyChanged}"/>

        <Label Content="New Price:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding NewPrice, UpdateSourceTrigger=PropertyChanged}"/>

        <Label Content="Активний:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center"/>
        <CheckBox Grid.Row="3" Grid.Column="1" IsChecked="{Binding Status}"/>

        <Label Content="Коментар:" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding Comment, UpdateSourceTrigger=PropertyChanged}"/>

        <Label Content="Дата:" Grid.Row="5" Grid.Column="0" VerticalAlignment="Center"/>
        <!-- Дата тільки для читання -->
        <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding Date, StringFormat=dd.MM.yyyy HH:mm}" VerticalAlignment="Center"/>

        <StackPanel Grid.Row="6" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Зберегти" Width="75" Margin="5" Click="SaveButton_Click"/>
            <Button Content="Відмінити" Width="75" Margin="5" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
